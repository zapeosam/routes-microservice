import { expect } from 'chai';

describe('model', () => {
    describe('getBestRoute', () => {

        beforeEach(() => {
        });

        afterEach(() => {
        });

        it('should return the best route for a given user', async () => {
            expect({}).to.deep.equal({});
        });

        it('should return an empty array if user does not exist', async () => {
            expect({}).to.deep.equal({});
        });        
    });
});